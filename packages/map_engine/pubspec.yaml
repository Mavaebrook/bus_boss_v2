name: map_engine
description: map_engine silo for the LYNX Bus Routing system.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  contracts:
    path: ../contracts
  riverpod: ^2.5.1
  rxdart: ^0.27.7
  equatable: ^2.0.5

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_map: ^6.1.0
  flutter_map_tile_caching: ^9.0.0
  vector_map_tiles: ^8.0.0
