name: transit_query_engine
description: transit_query_engine silo for the LYNX Bus Routing system.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  contracts:
    path: ../contracts
  transit_realtime:
    path: ../transit_realtime
  sqlite3: ^2.9.4
  sqlite3_flutter_libs: ^0.5.42
  riverpod: ^2.5.1
  rxdart: ^0.27.7
  equatable: ^2.0.5
  collection: ^1.18.0

dev_dependencies:
  flutter_lints: ^3.0.0
