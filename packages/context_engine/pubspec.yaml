name: context_engine
description: context_engine silo for the LYNX Bus Routing system.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  contracts:
    path: ../contracts
  riverpod: ^2.5.1
  rxdart: ^0.27.7
  equatable: ^2.0.5

dev_dependencies:
  flutter_lints: ^3.0.0
  dio: ^5.4.0
  cached: ^0.4.0
